document.addEventListener('DOMContentLoaded', function () {
    // Handle select-updated event
    // window.addEventListener('select-updated', event => {
    //     applyClassToSelectElement(event);
    // });

    // function applyClassToSelectElement(event) {
    //     const detail = Array.isArray(event.detail) ? event.detail[0] : event.detail;
    //     const elementx = document.querySelectorAll(`#${detail.selectId}`);

    //     elementx.forEach((el) => {
    //         console.log(el.classList);
    //         el.classList.add('xxxxxxxxxxxxxxxxxxxxxx12312123');
    //     })
    // }
});
